# `kconfig` In `ktools-java`

`ktools-java/` does not currently include a checked-in `kconfig`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `kconfig` is not yet implemented in the current Java workspace
- when it is added, this markdown file should be replaced by implementation
  docs from the Java workspace itself
