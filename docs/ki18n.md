# `ki18n` In `ktools-java`

`ktools-java/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` is not yet implemented in the current Java workspace
- the website reflects that status directly from this canonical markdown
