# Gamma Demo SDK

Exists for CI and as a minimal reference for integrating an SDK add-on with the
Java `ktrace` SDK.

Like alpha and beta, this SDK demonstrates the library-facing `TraceLogger`
definition pattern used by the Java API.
